BASE_IMAGE_TAG ?= latest
IMAGE_REPO = $(CI_REGISTRY_IMAGE)/fedora-$(FEDORA_ROOT_RELEASE)/jstest
IMAGE_BUILD_FROM = $(CI_REGISTRY_IMAGE)/fedora-$(FEDORA_ROOT_RELEASE)/build:$(BASE_IMAGE_TAG)

include ../build.mk
